# [2026-09-16] history | Documentation changes

- Prepared `semantic-atlas@2.3.1` with translation-aware diagram layout and
  corrected decision-node connections.
- Defined selectable, translatable HTML text alongside SVG diagram geometry in
  the shared Web/export Viewer, preserving one tracked source of business meaning.
- Unified initial and measured diagram layout so bilingual content grows cards,
  reroutes connections, and keeps decision edges on the actual diamond boundary.
- Documented existing Chinese-name and English-alias lookup support without
  introducing parallel translated map documents.
