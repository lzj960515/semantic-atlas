# [2026-09-18] revise | Navigate referenced business concepts

- Change: The shared Viewer opens a concept's owning business-domain diagram from its details. Cross-domain relationship nodes and concept-linked flow steps use the same action, while unlinked flow steps retain ordinary behavior. Card space displays a grab cursor, active dragging displays a grabbing cursor, and text retains its selection cursor in both diagram types.
- Reason: Cross-file references were already resolved and rendered, but a person could not follow a boundary concept into its owning domain. Flow concept references also had no inspection action.
- Evidence: [Viewer interactions](../../src/rendering/viewer-browser.ts), [flow projection](../../src/rendering/flow-projector.ts), and [projection regression](../../tests/rendering/map-projector.test.ts), checked against source version 2.5.0 on 2026-09-18.
- Affected pages: [Viewer ownership and navigation](../architecture.md#viewerpage).
- Verification: `pnpm release:verify` passed, including 175 tests, lint, format, typecheck, build, built-render/built-skill smoke checks, packaged-product verification, and package dry run. A headless Chrome interaction check exercised boundary-node navigation, flow concept navigation, keyboard activation, and an unlinked outcome; it reported no browser errors. The package has not been published or installed globally.
- Cursor follow-up: After the CSS adjustment, build, targeted formatting and Git diff checks passed. Browser interaction verified grab → grabbing → grab for relationship and flow cards, with text retaining its selection cursor after release. The local Web preview and offline AI map were regenerated.
- Related-flow follow-up: Flow details omit the currently displayed flow and hide the section when no other links remain; relationship details retain every related flow. The duplicate was reproduced in the local Web preview before the fix. After the fix, browser interaction verified the empty section is hidden, other flows remain navigable, and relationship view retains all links. Build and all 175 tests passed again; the Web preview and offline export were refreshed.
