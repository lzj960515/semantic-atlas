interface:
  display_name: "Semantic Atlas Maintenance"
  short_description: "Initialize business maps or reconcile one domain's candidates"
  default_prompt: "Use $semantic-atlas-maintenance to initialize the requested business map or reconcile retained candidates for one business domain, with source evidence and normal Git review."
