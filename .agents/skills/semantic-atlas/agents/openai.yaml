interface:
  display_name: "Semantic Atlas"
  short_description: "Build current-evidence business context for engineering work"
  default_prompt: "Use $semantic-atlas to understand this business-changing task with or without an existing business map, verify it in current evidence, and decide whether shared knowledge needs maintenance."
